import { StyleSheet, Text, View } from "react-native";

const header = () => {
  return (
    <View>
      <Text>header</Text>
    </View>
  );
};

export default header;

const styles = StyleSheet.create({});
