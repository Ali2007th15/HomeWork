#include <iostream>

using namespace std;

//int main() {
//    int m{}, n{};
//  cout << "Enter M: " << endl;
//    cin >> m;
//  cout << "Enter N: " << endl;
//    cin >> n;
//
//    int A[m];
//    cout << "Enter array A: " << endl;
//    for (int i = 0; i < m; ++i) {
//
//        cin >> A[i];
//    }
//    cout << "Enter array B: " << endl;
//    int B[n];
//    for (int i = 0; i < n; ++i) {
//
//        cin >> B[i];
//    }
//
//    int C[m];
//    int num1{};
//    for (int i = 0; i < m; ++i) {
//        int num2{};
//        for (int j = 0; j < n; ++j) {
//            num2 += (A[i] == B[j]);
//        }
//        if (!num2) {
//            int num3{};
//            for (int b = 0; b < num1; ++b) {
//                num3 += (A[i] == C[b]);
//            }
//            if (!num3) {
//                C[num1] = A[i];
//                ++num1;
//            }
//        }
//    }
//
//    for (int i = 0; i < num1; ++i) {
//        cout << C[i] << " ";
//    }
//    cout << endl;
//
//    return 0;
//}




int main() {
    int m{}, n{};
    cout << "Enter M: ";
    cin >> m;
    cout << "Enter N: ";
    cin >> n;

    int a[m], b[n];
    cout << "Enter array A: ";
    for (int i = 0; i < m; i++) {
        cin >> a[i];
    }
    cout << "Enter array B: ";
    for (int i = 0; i < n; i++) {
        cin >> b[i];
    }

    int c[m+n], len{};
    for (int i = 0; i < m; i++) {
        int j = 0;
        for (; j < n; j++) {
            if (a[i] == b[j]) {
                
            }
        }
        if (j == n) {
            int d{};
            for (; d < len; d++) {
                if (c[d] == a[i]) {
                    
                }
            }
            if (d == len) {
                c[len] = a[i];
                len++;
            }
        }
    }
    for (int i = 0; i < n; i++) {
        int j{};
        for (; j < m; j++) {
            if (b[i] == a[j]) {
                
            }
        }
        if (j == m) {
            int d{};
            for (; d < len; d++) {
                if (c[d] == b[i]) {
                    
                }
            }
            if (d == len) {
                c[len] = b[i];
                len++;
            }
        }
    }
    cout << "Elements that are not fit to arrays A and B: ";
    for (int i = 0; i < len; i++) {
        cout << c[i] << " ";
    }
    cout << endl;
    return 0;
}
