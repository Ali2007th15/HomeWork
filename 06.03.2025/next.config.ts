import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["www.alleycat.org", "cdn-icons-png.flaticon.com", "upload.wikimedia.org"],
  },
};

export default nextConfig;
